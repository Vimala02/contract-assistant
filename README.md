## Setup

### Setup

1. Create a `.env` file in the project root:

OPENAI_API_KEY=sk-your-key-here

2. Install dependencies:

pip install -r requirements.txt

3. Generate sample contracts, build index, and run the app.
